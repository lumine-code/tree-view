{
  "name": "tree-view",
  "author": "lumine-code",
  "version": "1.0.0",
  "description": "Explore and open project files in a tree-like view of your directories.",
  "keywords": [
    "sidebar",
    "filetree",
    "directory",
    "explorer",
    "file-manager"
  ],
  "repository": "https://github.com/lumine-code/tree-view",
  "bugs": {
    "url": "https://github.com/lumine-code/tree-view/issues"
  },
  "license": "MIT",
  "main": "./lib/main",
  "files": [
    "docs",
    "keymaps",
    "lib",
    "menus",
    "spec",
    "styles"
  ],
  "engines": {
    "lumine": "^1.0.0"
  },
  "backgroundTips": [
    "You can focus the Tree View without reaching for the mouse using {{ 'tree-view:toggle-focus' | keystroke }}",
    "You can reveal the file you are editing in the Tree View with {{ 'tree-view:reveal-active-file' | keystroke }}"
  ],
  "scripts": {
    "test": "lumine --test spec",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "configSchema": {
    "sortMethod": {
      "title": "Sort Method",
      "description": "Method used to sort files and directories in the tree view.",
      "type": "string",
      "enum": [
        {
          "value": "default",
          "description": "Default (locale-aware, numeric)"
        },
        {
          "value": "natural",
          "description": "Natural sort (alphabetical and numbers)"
        }
      ],
      "default": "default"
    },
    "sortFoldersBeforeFiles": {
      "title": "Sort Folders Before Files",
      "description": "When listing directory items, list subdirectories before listing files.",
      "type": "boolean",
      "default": true
    },
    "sortByBase": {
      "title": "Sort By Base",
      "description": "Sort by filename base and extension independently. Files with the same base name are grouped together.",
      "type": "boolean",
      "default": true
    },
    "duplicateCopyNameStyle": {
      "title": "Duplicate Copy Name Style",
      "description": "Naming style used when copy-pasting an entry where an item with the same name already exists.",
      "type": "string",
      "enum": [
        {
          "value": "system",
          "description": "System default (Windows, macOS, or Linux style)"
        },
        {
          "value": "legacy",
          "description": "Legacy tree-view style: file0.txt, file1.txt"
        },
        {
          "value": "windows",
          "description": "Windows style: file - Copy.txt, file - Copy (2).txt"
        },
        {
          "value": "macos",
          "description": "macOS style: file copy.txt, file copy 2.txt"
        },
        {
          "value": "linux",
          "description": "Linux/GNOME style: file (copy).txt, file (another copy).txt"
        }
      ],
      "default": "system"
    },
    "showOnRightSide": {
      "title": "Show On Right Side",
      "description": "Show the tree view on the right side of the workspace.",
      "type": "boolean",
      "default": false
    },
    "squashDirectoryNames": {
      "title": "Collapse directories",
      "description": "Collapse directories that only contain a single directory.",
      "type": "boolean",
      "default": false
    },
    "treeAppearance": {
      "title": "Tree Appearance",
      "description": "Choose how directory hierarchy and expansion controls are displayed.",
      "type": "string",
      "enum": [
        {
          "value": "guides",
          "description": "Indent Guides — always-visible rails with integrated directory controls"
        },
        {
          "value": "classic",
          "description": "Classic — conventional chevrons without indent guides"
        }
      ],
      "default": "classic"
    },
    "stickyHeaders": {
      "title": "Sticky Directory Headers",
      "description": "Choose which directories stay pinned above the tree while scrolling.",
      "type": "string",
      "enum": [
        {
          "value": "none",
          "description": "None — nothing is pinned while scrolling"
        },
        {
          "value": "roots",
          "description": "Roots — pin the project root only"
        },
        {
          "value": "directories",
          "description": "Directories — pin the project root and every expanded ancestor directory"
        }
      ],
      "default": "none"
    },
    "hideVcsIgnoredFiles": {
      "title": "Hide VCS Ignored Files",
      "description": "Don't show files and directories ignored by the current project's VCS system. For example, projects using Git have these paths defined in their `.gitignore` file.",
      "type": "boolean",
      "default": false
    },
    "hideIgnoredNames": {
      "title": "Hide Ignored Names",
      "description": "Don't show items matched by the `Ignored Names` core config setting.",
      "type": "boolean",
      "default": false
    },
    "autoReveal": {
      "title": "Auto Reveal",
      "description": "Reveal tree view entries when they become the active pane item.",
      "type": "boolean",
      "default": false
    },
    "focusOnReveal": {
      "title": "Focus On Reveal",
      "description": "Focus the tree view when revealing entries.",
      "type": "boolean",
      "default": true
    },
    "alwaysOpenExisting": {
      "title": "Always Open Existing",
      "description": "When opening a file, always focus an already-existing view of the file even if it's in another pane.",
      "type": "boolean",
      "default": false
    },
    "hiddenOnStartup": {
      "title": "Hidden On Startup",
      "description": "When Lumine is opened, the view is hidden.",
      "type": "boolean",
      "default": false
    },
    "fileDoubleClick": {
      "title": "File Double Click",
      "description": "Require double click to open files. When enabled, single click only selects the entry.",
      "type": "boolean",
      "default": false
    },
    "dirDoubleClick": {
      "title": "Dir Double Click",
      "description": "Require double click to expand/collapse folders. When enabled, single click only selects the entry. The disclosure control always responds to a single click.",
      "type": "boolean",
      "default": false
    }
  },
  "providedServices": {
    "tree-view.selection": {
      "description": "Tree view API with path selection, entry lookup, and reveal support",
      "versions": {
        "1.0.0": "provideTreeViewSelection"
      }
    },
    "tree-view.roots": {
      "description": "Register virtual root sections in the tree view",
      "versions": {
        "1.0.0": "provideTreeViewRoots"
      }
    }
  },
  "consumedServices": {
    "busy-signal": {
      "versions": {
        "^1.0.0": "consumeBusySignal"
      }
    },
    "open-external": {
      "versions": {
        "^1.0.0": "consumeOpenExternal"
      }
    },
    "project-list": {
      "versions": {
        "^1.0.0": "consumeProjectList"
      }
    },
    "recent-list": {
      "versions": {
        "^1.0.0": "consumeRecentList"
      }
    }
  },
  "deserializers": {
    "TreeView": "getTreeViewInstance"
  },
  "dependencies": {
    "natural-compare-lite": "^1.4.0",
    "picomatch": "^4.0.5"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.1",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-n": "^18.3.0",
    "globals": "^17.11.0",
    "prettier": "^3.9.6"
  }
}
